name: CI

on: [push]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        #os: [macOS-latest, ubuntu-latest, windows-latest]
        os: [ubuntu-16.04]
    steps:
    - uses: actions/checkout@v1
    - name: Build and test container
      run: |
        chmod +x test.sh
        ./test.sh
      shell: bash
